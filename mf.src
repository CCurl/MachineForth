\ TEST

: space $20 drop #32 drop %00100000 drop 32 emit ;
: ok space 'o' emit 'k' emit ;
: name dup if 1+ then emit ;
: test 1000 1000 * 500 * begin 1- if drop ret then again ;
: main ok test ok ;
